e:\se\BlindDuel\plans\祭坛状态物件设计.MD
# 祭坛状态物件设计（PropEntity 扩展为状态物件）

> **状态：已完成并归档（2026-07-15）。** 设计正文（§1-§10）作为设计参考保留；实施记录见 [commits_detailed/26.7.13 Altar added.MD](./commits_detailed/26.7.13%20Altar%20added.MD) 与 [commits_detailed/26.7.14 Altar Quest.MD](./commits_detailed/26.7.14%20Altar%20Quest.MD)。
> 把 PropEntity 从"过场一次性角色"扩展为"可挡路 + 可遮挡 + scenario/flag 驱动切 clip 的状态物件"，首个用例是 prologue 祭坛（init ↔ completed）。
> **关联**：[Prologue 收尾三项设计.MD](./Prologue%20收尾三项设计.MD)、[PROJECT_CONTEXT.md §6.13/6.14](../PROJECT_CONTEXT.md)。

---

## 0. 背景与目标

Prologue 有个任务：主人公捡道具交 Charlotte，Charlotte 复原祭坛。需求是祭坛交任务前后视觉变化（台面无钵 → 有钵）。

方案选择过程：
- NpcCharacter 方案：杀鸡用牛刀（强制带 NpcController/occupancy/interactables，stateGraph clip 切换无人驱动）
- 新建 SceneryEntity：与 PropEntity 高度重叠，过度设计
- **本方案：扩展现有 PropEntity**，加 blocker/depthMask/stateMap 三个能力，复用现有 actorRegistry/ExploreCollisionSystem 体系

未来同模式复用：宝箱（空/满）、门（关/开）、机关（未触发/已触发）、篝火（灭/燃）。一次性投入，长期收益。

---

## 1. 现状缺口

### 1.1 PropEntity 无占位能力
- [PropEntity.js](file:///e:/se/BlindDuel/scripts/Enties/PropEntity.js) 无 `getBlockerAabb()`
- [ExploreMode._buildIndices](file:///e:/se/BlindDuel/scripts/Systems/Modes/ExploreMode.js#L405-L449) staticBlockers 只收 `kind==="npc" && blocksMovement`，PropEntity 即使实现接口也不被收集
- 无 occupancy 扫描依赖（PropEntity root 来自 `config.pos` + 固定几何偏移，和 rabble 一致）

### 1.2 PropEntity 无遮挡能力
- 现有遮挡走 [SceneVisualSystem.createDepthMasks](file:///e:/se/BlindDuel/scripts/Enties/SceneVisualSystem.js#L34-L113)，按整张场景背景扫描，场景级静态、不随实体存在性变化
- STAGE 层 `parallaxFactor: 1.0`（[SceneVisualSystem.js:586](file:///e:/se/BlindDuel/scripts/Enties/SceneVisualSystem.js#L586)），`layerOffsetX = cameraAnchorX * (1 - 1.0) = 0`，本就不滚动；祭坛与 hero 同属 STAGE 层语义，钉在世界坐标，无背景视差移动

### 1.3 PropEntity 无状态驱动
- clip 切换靠 sequence SEND_COMMAND（手动），无 scenario/flag 自动响应
- NpcCharacter 的 stateGraph 也是手动 enterState，同样不自动响应 WorldState

### 1.4 Scene._onWorldStateChange 只处理 _pendingSpawns
- [Scene.js:451-461](file:///e:/se/BlindDuel/scripts/Scene.js#L451-L461) 当前只检查未生成的 entity，不处理已存在 entity 的状态切换

### 1.5 PropEntity 只支持多文件 clip，不支持单文件多 tag
- [PropEntity._setClip](file:///e:/se/BlindDuel/scripts/Enties/PropEntity.js#L58-L113) 每个 clip 独立 `spriteSheetUrl` + `atlasData`，切 clip 时 `material.diffuseTexture.dispose()` + `new Texture(...)`（[L95-L97](file:///e:/se/BlindDuel/scripts/Enties/PropEntity.js#L95-L97)），texture 异步加载会有闪烁
- [NpcFrameComponent](file:///e:/se/BlindDuel/scripts/Components/NpcFrameComponent.js#L40-L56) 已支持单文件多 tag（`atlas.meta.frameTags` + clip 的 `from/to`），texture 共享不换。PropEntity 未跟进
- 大尺寸 prop（祭坛/宝箱/门）多状态切文件会闪、占显存、美术拆文件工作流不友好；应复用 NPC 的 tag 机制

### 1.6 WalkArea 不支持 scenario 驱动变化
- [Scene.js:166-175](file:///e:/se/BlindDuel/scripts/Scene.js#L166-L175) 只支持单一 `walkArea` 或 StageMask 里的 `walkArea`，init 时创建后固定不变
- [WalkArea.js](file:///e:/se/BlindDuel/scripts/Enties/WalkArea.js) 无 `updateBounds()` 方法，无动态更新能力
- prologue 需要 scenario 推进后扩展玩家可行走区域（例如祭坛复原后解锁右侧区域）

---

## 2. 设计要点

### 2.1 状态物件（PropEntity）决策表

| 维度 | 决策 |
|---|---|
| 实体类型 | 复用 PropEntity，不新建类 |
| root 语义 | sprite 底边中点在地面投影（PropEntity 类硬编码，与 rabble 一致） |
| 碰撞 | `blocker` AABB 配置，进 ExploreCollisionSystem.staticBlockers |
| 遮挡 | `depthMask` AABB 配置，建 stencil plane 挂 root（与 sprite 同父；STAGE 层 parallaxFactor=1.0 不滚动，自然同步） |
| 钵 | 画进 completed clip 的 sprite 内部，被主体包裹，无独立遮挡/sprite |
| clip 切换 | stateMap 不动 depthMask，只切 sprite texture |
| 状态驱动 | `stateMap: [{if, clip}]` 有序条件数组，复用 `_evaluateCondition`，支持 scenario/flag 混用 |
| 驱动时机 | Scene._onWorldStateChange + 首次 spawn 评估 |
| 初始 clip | stateMap 首个命中 > initialClip 兜底 > clips 首项 |
| 交互 | 无（祭坛纯视觉） |

### 2.2 WalkArea 决策表

| 维度 | 决策 |
|---|---|
| 数据结构 | `walkAreas: [{if, minX, maxX, minY, maxY}]` 有序条件数组 |
| 条件语法 | 复用 `_evaluateCondition`（flag/scenario/quest 混用），与 stateMap/spawnIf 同源 |
| 初始评估 | Scene.init 创建后立刻 `_applyWalkArea()` 取首个命中 |
| 动态响应 | WorldState 变化时 `_onWorldStateChange` → `_applyWalkArea()` 重评估 |
| 重建策略 | 命中新边界时 `walkArea.dispose()` + `new WalkArea(...)`，低频事件无需 incremental 更新 |
| 兼容旧格式 | `walkArea` 单对象自动转成 `[{if:{}, ...walkArea}]` |
| StageMask 优先 | 若 StageMask 提供 walkArea，仍按原优先级使用，转成 `[{if:{}, ...stageMaskWalkArea}]` |

### 2.3 root 锚点（与 rabble 一致）

[PropEntity.js:17](file:///e:/se/BlindDuel/scripts/Enties/PropEntity.js#L17) `this.root.position.set(pos[0], pos[1], pos[2])`，[PropEntity.js:53](file:///e:/se/BlindDuel/scripts/Enties/PropEntity.js#L53) `spritePlane.position.y = planeH / 2`。

→ root = sprite 底边中点在地面投影。`pos[1]=0` 让 sprite 底边贴地。不依赖扫描数据（PropEntity 全文不读 rootMotion/occupancy）。

### 2.4 blocker 字段（碰撞盒，相对 root 本地坐标）

```jsonc
"blocker": { "halfW": 1.2, "halfH": 0.9, "centerY": 0.9 }
```

- `centerY`：盒子几何中心相对 root 的 y（世界坐标）。台面贴地则 `centerY = halfH`；台面悬空则 `centerY = halfH + 悬空高度`
- 与 AABBTrigger.pos 语义一致（中心点）

PropEntity 实现：
```js
getBlockerAabb() {
    if (!this._blocker) return null;
    const p = this.root.position;
    const b = this._blocker;
    return {
        minX: p.x - b.halfW, maxX: p.x + b.halfW,
        minY: b.centerY - b.halfH, maxY: b.centerY + b.halfH
    };
}
```

### 2.5 depthMask 字段（遮挡盒，相对 root 本地坐标）

```jsonc
"depthMask": { "halfW": 1.3, "halfH": 2.0, "centerY": 1.0 }
```

字段结构与 blocker 完全一致，语义不同：
- blocker：碰撞盒（挡路），由 ExploreCollisionSystem 消费
- depthMask：遮挡盒（走背后变透明），由 stencil plane 实现

**关键设计**：depthMask plane 挂 `this.root`（与 spritePlane 同父）。STAGE 层 `parallaxFactor: 1.0`（[SceneVisualSystem.js:586](file:///e:/se/BlindDuel/scripts/Enties/SceneVisualSystem.js#L586)），`layerOffsetX` 恒为 0，本就不滚动——祭坛与 hero 同属 STAGE 层语义，钉在世界坐标，无背景视差移动：
- depthMask 与 sprite 同步移动（root 动两者一起动）
- STAGE 层不滚动，hero 走动时祭坛与 depthMask 都钉死，stencil 遮挡始终对齐 sprite

stencil 实现复用 SceneVisualSystem.createDepthMasks 的 `onBeforeRenderObservable`/`onAfterRenderObservable` 逻辑，抽成工具函数：

```js
// scripts/Utils/StencilOccluder.js（新增，或挂 SceneVisualSystem 静态方法）
export function attachStencilOccluder(plane, scene) {
    const gl = scene.getEngine()._gl;
    plane.onBeforeRenderObservable.add(() => {
        gl.colorMask(false, false, false, false);
        gl.enable(gl.STENCIL_TEST);
        gl.stencilMask(0xFF);
        gl.stencilFunc(gl.ALWAYS, 1, 0xFF);
        gl.stencilOp(gl.KEEP, gl.KEEP, gl.REPLACE);
    });
    plane.onAfterRenderObservable.add(() => {
        gl.colorMask(true, true, true, true);
        gl.stencilFunc(gl.NOTEQUAL, 1, 0xFF);
        gl.stencilOp(gl.KEEP, gl.KEEP, gl.KEEP);
        gl.stencilMask(0x00);
    });
}
```

PropEntity._buildSpritePlane 末尾，若 config.depthMask 存在则建一个 stencil plane 并 attach。

### 2.6 stateMap 字段（状态驱动 clip 切换）

```jsonc
"stateMap": [
  { "if": { "scenarioMin": 105 }, "clip": "completed" },
  { "if": {}, "clip": "init" }
]
```

- 有序数组，从上往下匹配，**首个 `if` 命中生效**
- `if` 复用 [Scene._evaluateCondition](file:///e:/se/BlindDuel/scripts/Scene.js#L462-L475) 语法（flag/flagNot/scenario/scenarioMin/scenarioMax/quest），与 spawnIf/trigger condition 同源
- 多触发源混用：祭坛用 scenarioMin，宝箱用 flag，门用 flag 链

宝箱示例：
```jsonc
"stateMap": [
  { "if": { "flag": "chest_1_opened" }, "clip": "open" },
  { "if": {}, "clip": "closed" }
]
```

门示例（关/开两态）：
```jsonc
"stateMap": [
  { "if": { "flag": "door_a_open" }, "clip": "open" },
  { "if": {}, "clip": "closed" }
]
```

### 2.7 clip 模式约定（单文件多 tag vs 多文件）

PropEntity 支持两种 clip 配置方式，**祭坛等大尺寸状态物件优先用单文件多 tag**：

**方式 A：单文件多 tag（推荐，复用 [NpcFrameComponent](file:///e:/se/BlindDuel/scripts/Components/NpcFrameComponent.js#L40-L56) 机制）**
```jsonc
"spriteSheetUrl": "./Art/Environment/altar.png",
"atlasData": { /* meta.frameTags: [{name:"init",from:0,to:0},{name:"completed",from:1,to:1}] */ },
"clips": {
  "init":      { "tag": "init",      "mode": "hold" },
  "completed": { "tag": "completed", "mode": "hold" }
}
```
- texture 在 entity 级建一次，切 clip 只改 `currentFrameIndex` + `_applyFrame`，**零 texture 切换、零闪烁**
- fixedUpdate 循环/hold 边界限制在 `tag.from ~ tag.to`
- 美术在 ase 同一文件画所有状态帧 + 设 frameTags，导出一份 png + json

**方式 B：多文件（旧式，rabble 保持不变，兼容）**
```jsonc
"clips": {
  "idle": { "spriteSheetUrl": "rabble_idle.png", "atlasData": {...} },
  "fall": { "spriteSheetUrl": "rabble_fall.png", "atlasData": {...} }
}
```
- clip 有 `spriteSheetUrl` 走旧逻辑（换 texture）
- 判断：clip 有 `tag` 字段 → 共享 entity 级 texture；clip 有 `spriteSheetUrl` → 独立 texture

**mode 约定**：静态状态（祭坛 init/completed）用单帧 atlas + `mode:"hold"`。若未来要"钵渐变出现"效果，completed clip 改为多帧动画 + `mode:"hold"`，播完自动定格末帧——无需新增"按帧停止"能力，现有 hold 模式即可。

### 2.8 walkAreas 字段（状态驱动 WalkArea 边界）

```jsonc
"walkAreas": [
  { "if": { "scenarioMin": 105 }, "minX": -24, "maxX": 15, "minY": -1, "maxY": 0.7 },
  { "if": {}, "minX": -24, "maxX": -7, "minY": -1, "maxY": 0.7 }
]
```

- 有序数组，从上往下匹配，首个命中生效
- 命中后若边界与当前不同，重建 WalkArea（低频事件，无需 incremental 更新）
- 旧格式 `walkArea` 单对象自动兼容（转成 `[{if:{}, ...walkArea}]`）
- StageMask walkArea 优先级最高，转成 `[{if:{}, ...stageMaskWalkArea}]` 放数组首位

### 2.9 钵的视觉约束（美术侧）

- completed 帧 = 祭坛主体 + 钵（钵画在帧内部）
- init 帧 = 祭坛主体（钵不存在）
- 两帧在同一 atlas（单文件多 tag），外轮廓一致（都是主体轮廓），只是内部多了/少了钵
- 钵位置完全被祭坛主体 sprite 包裹 → 遮挡关系随主体，depthMask 只需一个 AABB（主体轮廓），clip 切换不动遮罩

---

## 3. 驱动时机

### 3.1 PropEntity stateMap 首次 spawn 评估

[Scene._spawnEntity](file:///e:/se/BlindDuel/scripts/Scene.js#L395-L428) 末尾，若 entity 有 stateMap：
```js
if (entity.stateMap) {
    this._applyStateMap(entity);
}
```

`_applyStateMap` 实现：
```js
_applyStateMap(entity) {
    if (!entity.stateMap) return;
    for (const entry of entity.stateMap) {
        if (this._evaluateCondition(entry.if, this.worldState)) {
            if (entity.currentStateName !== entry.clip) {
                entity.enterState(entry.clip);
            }
            return;
        }
    }
    // 全不命中：回退 initialClip 或 clips 首项
    const fallback = entity._initialClip ?? Object.keys(entity.clips)[0];
    if (fallback && entity.currentStateName !== fallback) {
        entity.enterState(fallback);
    }
}
```

### 3.2 WalkArea 首次 init 评估

[Scene.js:166-175](file:///e:/se/BlindDuel/scripts/Scene.js#L166-L175) 改为：
```js
const rawWalkAreaDef = stageMaskData?.walkArea
    ? {
          minX: stageMaskData.walkArea.x,
          maxX: stageMaskData.walkArea.x + stageMaskData.walkArea.w,
          minY: stageMaskData.walkArea.y,
          maxY: stageMaskData.walkArea.y + stageMaskData.walkArea.h,
      }
    : sceneDef.walkArea;

if (sceneDef.walkAreas) {
    this._walkAreas = sceneDef.walkAreas;
} else if (rawWalkAreaDef) {
    this._walkAreas = [{ if: {}, ...rawWalkAreaDef }];
}

this._applyWalkArea();
```

`_applyWalkArea` 实现：
```js
_applyWalkArea() {
    if (!this._walkAreas?.length) return;

    for (const def of this._walkAreas) {
        if (this._evaluateCondition(def.if, this.worldState)) {
            const current = this.walkArea;
            if (!current ||
                current.minX !== def.minX || current.maxX !== def.maxX ||
                current.minY !== def.minY || current.maxY !== def.maxY) {
                if (current) current.dispose();
                this.walkArea = new WalkArea(this.scene, {
                    minX: def.minX, maxX: def.maxX,
                    minY: def.minY, maxY: def.maxY,
                    visible: true
                });
                this._sharedContext.walkArea = this.walkArea;
            }
            return;
        }
    }
}
```

### 3.3 WorldState 变化时重评估

[Scene._onWorldStateChange](file:///e:/se/BlindDuel/scripts/Scene.js#L451-L461) 扩展：在 _pendingSpawns 循环后加一段：
```js
// 重评估 stateMap
for (const entity of this.entityPool) {
    if (entity.stateMap && !entity.isDisposed) {
        this._applyStateMap(entity);
    }
}

// 重评估 walkarea
this._applyWalkArea();
```

仅当新命中 clip 与 currentStateName 不同时才 enterState，避免无谓 texture 切换；仅当新命中边界与当前不同时才重建 WalkArea，避免无谓重建。

### 3.4 _evaluateCondition 复用

[Scene._evaluateCondition](file:///e:/se/BlindDuel/scripts/Scene.js#L462-L475) 已支持 flag/flagNot/scenario/scenarioMin/scenarioMax/quest，stateMap 和 walkAreas 的 `if` 都直接走它，零新增条件语法。

---

## 4. entityDef 配置示例

### 4.1 祭坛（prologue，单文件多 tag 模式）

```jsonc
{
  "archetype": "prop",
  "id": "altar",
  "name": "altar",
  "kind": "prop",
  "pos": [4, 0, 0],
  "controller": "none",
  "blocksMovement": true,
  "blocker": { "halfW": 1.2, "halfH": 0.9, "centerY": 0.9 },
  "depthMask": { "halfW": 1.3, "halfH": 2.0, "centerY": 1.0 },
  "spriteSheetUrl": "./Art/Environment/altar.png",
  "atlasData": { /* meta.frameTags: [{name:"init",from:0,to:0},{name:"completed",from:1,to:1}] */ },
  "initialClip": "init",
  "clips": {
    "init":      { "tag": "init",      "mode": "hold" },
    "completed": { "tag": "completed", "mode": "hold" }
  },
  "stateMap": [
    { "if": { "scenarioMin": 105 }, "clip": "completed" },
    { "if": {}, "clip": "init" }
  ]
}
```

### 4.2 宝箱（未来，flag 驱动示范）

```jsonc
{
  "archetype": "prop",
  "id": "chest_1",
  "pos": [-2, 0, 0],
  "controller": "none",
  "blocksMovement": true,
  "blocker": { "halfW": 0.5, "halfH": 0.4, "centerY": 0.4 },
  "depthMask": { "halfW": 0.55, "halfH": 0.6, "centerY": 0.4 },
  "stateMap": [
    { "if": { "flag": "chest_1_opened" }, "clip": "open" },
    { "if": {}, "clip": "closed" }
  ]
}
```

### 4.3 WalkArea 动态扩展（prologue 祭坛右侧区域解锁）

```jsonc
"walkAreas": [
  { "if": { "scenarioMin": 105 }, "minX": -24, "maxX": 15, "minY": -1, "maxY": 0.7 },
  { "if": {}, "minX": -24, "maxX": -7, "minY": -1, "maxY": 0.7 }
]
```

scenario<105 时玩家只能在左侧 `[-24, -7]` 走动；交任务 advanceScenario(105) 后解锁右侧 `[ -7, 15]`（祭坛区域）。

---

## 5. 代码改动点

### 5.1 PropEntity.js（核心扩展）

1. 构造函数：读 `config.blocker` / `config.depthMask` / `config.stateMap` / `config.initialClip` / `config.spriteSheetUrl` / `config.atlasData`（entity 级共享 texture）
2. `_buildSpritePlane`：末尾若 depthMask 存在，建 stencil plane（挂 root，attachStencilOccluder）
3. 新增 `getBlockerAabb()`（§2.4）
4. `_setClip` 扩展多 tag 模式：clip 有 `tag` 字段 → 不换 texture，按 `atlas.meta.frameTags` 找 from/to，设 `currentFrameIndex = from` + `_applyFrame(from)`；clip 有 `spriteSheetUrl` → 走旧逻辑换 texture
5. `fixedUpdate` 扩展 tag 边界：循环/hold 限制在 `tag.from ~ tag.to`（复用 [NpcFrameComponent.fixedUpdate](file:///e:/se/BlindDuel/scripts/Components/NpcFrameComponent.js#L98-L123) 逻辑）
6. `_applyStateMap` 不放 PropEntity（放 Scene，因要访问 worldState + _evaluateCondition）
7. dispose：销毁 stencil plane + material + entity 级 texture（若存在）

### 5.2 Scene.js（驱动 + 收集）

1. `_spawnEntity` 末尾调 `_applyStateMap(entity)`（§3.1）
2. init 中解析 `walkAreas` 或兼容 `walkArea`，并调 `_applyWalkArea()`（§3.2）
3. `_onWorldStateChange` 扩展：stateMap 重评估 + walkarea 重评估（§3.3）
4. 新增 `_applyStateMap(entity)` 方法（§3.1）
5. 新增 `_applyWalkArea()` 方法（§3.2）

### 5.3 ExploreMode.js（staticBlockers 收集）

[_buildIndices](file:///e:/se/BlindDuel/scripts/Systems/Modes/ExploreMode.js#L405-L449)：
```js
// 现状：只收 kind==="npc" && blocksMovement
if (entity.kind === "npc" && entity.blocksMovement) {
    this.staticBlockers.push(entity);
}
// 改为：prop 也收（只要实现了 getBlockerAabb）
if ((entity.kind === "npc" || entity.kind === "prop") 
    && entity.blocksMovement 
    && typeof entity.getBlockerAabb === "function") {
    this.staticBlockers.push(entity);
}
```

### 5.4 StencilOccluder 工具函数（新增）

`scripts/Utils/StencilOccluder.js` 或挂 SceneVisualSystem 静态方法。SceneVisualSystem.createDepthMasks 同步改用此工具（去重）。

### 5.5 CharacterFactory.createPropEntity（透传新字段）

[createPropEntity](file:///e:/se/BlindDuel/scripts/CharacterFactory.js#L457-L490) 把 entityDef.blocker/depthMask/stateMap/initialClip/spriteSheetUrl/atlasData 透传进 PropEntity config。

**注意**：现有多文件模式下 clips 内联 spriteSheetUrl/atlasData（每 clip 独立），新 tag 模式下 spriteSheetUrl/atlasData 提升到 entity 级、clips 只配 tag。createPropEntity 需识别两种结构并透传。

---

## 6. 与 prologue 任务流的衔接

完整链路：

1. 玩家拾取 dagger（PickableEntity，已就绪）
2. 玩家持 dagger 走近 Charlotte → NpcController greeting → action: `removeItem:dagger` + `advanceScenario:105`
3. QuestManager.advanceTo(105) → WorldState.setScenario(105) → _notify
4. Scene._onWorldStateChange 同时触发：
   - 祭坛 stateMap 命中 `scenarioMin:105` → enterState("completed") → sprite 切 texture
   - walkAreas 命中 `scenarioMin:105` → 重建 WalkArea，扩展右侧可走区域
5. 视觉效果：祭坛台面出现钵，玩家可继续向右探索

**Charlotte 收物动画（可选，解耦）**：若后续觉得纯切 clip 不够，在 NpcDefs.companion 加 `receiveClip` 字段，NpcController greeting 触发 action 时让她播一段接收动画。不阻塞本方案。

---

## 7. 风险与边界

| 风险 | 对策 |
|---|---|
| depthMask plane 与 sprite 不同步移动 | depthMask 挂 root（与 sprite 同父），保证同步 |
| stencil plane 渲染顺序错乱 | renderingGroupId=1 + alphaIndex 取较大值（参考 SceneVisualSystem 用 10000），确保在角色前绘制 |
| stateMap 全不命中时无 clip | 回退 initialClip，再回退 clips 首项（§3.1） |
| prop 被 dispose 后 stateMap 仍被扫 | _onWorldStateChange 检查 `!entity.isDisposed` |
| 同帧多个 stateMap 命中 | 有序数组首个命中即 return，语义明确 |
| PropEntity 进 staticBlockers 后与 StageMask pushbox 重叠 | 美术侧避免祭坛与 StageMask 障碍物重叠；运行时 AABB 重叠无 bug（都挡路） |
| WalkArea 切换时玩家正站在新区域外 | WalkArea 只约束未来移动，不瞬移玩家；若玩家已在新区域外，继续走会被 clamp 拉回边界，这是预期行为 |
| WalkArea 切换频繁重建开销 | scenario/flag 切换是低频事件，重建一次 WalkArea 开销可忽略；_applyWalkArea 只在边界真的变化时才重建 |
| StageMask walkArea 与 walkAreas 混用 | StageMask 扫描结果转成 `[{if:{}, ...}]` 放数组首位，保证美术扫描结果优先级最高；通常不建议同时混用 |

---

## 8. 验证清单

### 碰撞
- [ ] 祭坛 `blocksMovement:true` 时 hero 无法穿过台面
- [ ] 祭坛 `blocksMovement:false` 时不挡路
- [ ] blocker.centerY 调整后碰撞盒位置正确（debug 可视化）

### 遮挡
- [ ] hero 走到祭坛背后时被 stencil 遮挡（变透明/不可见）
- [ ] hero 走出祭坛遮挡区域恢复正常
- [ ] hero 走动时 depthMask 与 sprite 始终对齐（无视差错位）
- [ ] 钵所在区域被主体 depthMask 覆盖（hero 走到钵背后也被遮挡）

### 状态切换
- [ ] scenario<105 时祭坛显示 init clip
- [ ] 交任务 advanceScenario(105) 后祭坛切 completed clip
- [ ] 切换不导致 depthMask 位置/大小变化（钵被主体包裹）
- [ ] 重进场景（scenario 已 105）祭坛首次 spawn 即 completed
- [ ] stateMap 全不命中时回退 initialClip

### WalkArea 动态变化
- [ ] scenario<105 时 walkarea 限制在左侧区域
- [ ] 交任务 advanceScenario(105) 后 walkarea 扩展到右侧区域
- [ ] hero 走出新边界时被 clamp 拉回
- [ ] 旧格式 `walkArea` 仍兼容
- [ ] 重进场景（scenario 已 105）首次 spawn 即使用扩展边界

### 任务流
- [ ] 拾取 altar_gem → 交 Charlotte → 祭坛切 completed + walkarea 扩展，全链路通畅
- [ ] Charlotte greeting 正常（不受祭坛方案影响）

### 复用验证
- [ ] 宝箱配置（flag 驱动）按 stateMap 正确切 clip
- [ ] 门配置（flag 链）按 stateMap 正确切 clip
- [ ] 宝箱 flag 驱动也正确切换 walkarea（如需要）

---

## 9. 开放问题（待拍板）

1. **stencil plane 是否需要 debug 可视化？** SceneVisualSystem 的 mask 有 _debugPanel。PropEntity 的 depthMask 建议也加一个可 toggle 的线框（C 键统一显隐），便于配置 AABB 时校对。**倾向：加，复用 CharacterBase 的 debug 模式。**
2. **stateMap 是否需要支持"切换时播一段过渡 clip"？** 例如门 open 时先播 opening 动画再停在 open。当前方案是瞬切。**建议：本次不做，未来按需扩 `transitionClip` 字段。**
3. **PropEntity 是否需要 visibleIf（与 spawnIf 并列）？** BACKLOG 已列。当前祭坛用 spawnIf（不存在 vs 存在两态）够用。若未来要"存在但不可见"（如未触发时半透明轮廓），再加 visibleIf。**本次不做。**
4. **depthMask 是否支持每 clip 独立 AABB？** 当前方案是一个 AABB 不随 clip 变。若未来"祭坛碎裂"整体轮廓大变，再扩 `clips[].depthMask`。**本次不做。**
5. **Charlotte receiveClip 触发链路细节。** 若做，在 NpcController._triggerAction 之后、enterIdle 之前插入 `if (npcDef.receiveClip) npc.animation.play(receiveClip)`，并在动画结束后才 enterIdle。需 NpcController 加状态（receiveing）。**建议：本次不做，留待 Charlotte 美术补帧时一并设计。**
6. **walkAreas 是否需要支持平滑过渡？** 当前是 dispose+重建，玩家会被新边界硬 clamp。若未来要做"门缓缓打开、可行走区域随之扩展"的演出，可改用 `WalkArea.updateBounds()` 做 incremental 缩放 + 每帧重新 clamp。**建议：本次不做，重建足够。**

---

## 10. 实施顺序建议

### 10.0 决策落地（已确认）

| # | 决策 | 实际值 |
|---|---|---|
| D1 | prologue 拾取物 | `dagger` → `altar_gem`（整体替换，不并存） |
| D2 | 祭坛 frameTag 名 | 沿用美术导出 `init` / `completed`（§4.1/§2.6/§2.7/§2.9 等文档示例已统一为实际命名） |
| D3 | Charlotte 收物链路 | [NpcDefs.companion priority 90](file:///e:/se/BlindDuel/Data/NpcDefs.js#L23-L28) 已配 `action:[removeItem, advanceScenario:105]`，QuestManager.executeDirectives 已支持；迁移把 `item:"dagger"` 改 `"altar_gem"`。`giveItem:"dagger"` 删除（设计上 Charlotte 收物后无回礼，只推进 scenario） |

**资源核查结论**：`altar.png`/`altar.json`/`altar_gem.png`/`altar_gem.json` 均已就绪。

### 10.1 迁移影响面（dagger → altar_gem）

| 文件 | 改动 |
|---|---|
| `Art/Sprite/items/altar_gem.json` | 已就绪（参照 [dagger.json](file:///e:/se/BlindDuel/Art/Sprite/items/dagger.json)，32×32 单帧） |
| `scripts/AssetManifest.js` | items 注册 `altar_gem` |
| `Data/ItemDefs.js` | 新增 `altar_gem` def（若 InventoryManager 走 def 路径） |
| `Data/SceneDefs/prologue.json` | `dagger_01` pickable → `altar_gem_01`，itemDef.id/atlasKey/textureUrl 全改 |
| `Data/NpcDefs.js` | companion priority 90：`hasItem`/`removeItem` 改 `altar_gem`；**删除 `giveItem:"dagger"`**（D3 决策落地） |

### 10.2 分阶段实施顺序（按依赖 + 可测试性）

**Phase 0 — 资源补齐（无运行时影响）**
- 0.1 ~~新建 `Art/Sprite/items/altar_gem.json`~~（已完成，资源已就绪）

**Phase 1 — 工具底层**
- 1.1 新建 `scripts/Utils/StencilOccluder.js`（§2.5）
- 1.2（可选）SceneVisualSystem.createDepthMasks 改用 1.1 工具去重

**Phase 2 — PropEntity 能力扩展**（2.1/2.2/2.3 可并行，2.4 依赖 2.1+2.3，2.5 是回归保障）
- 2.1 单文件多 tag 模式：`_setClip` 识别 `clip.tag` → entity 级共享 texture + `meta.frameTags` 取 from/to；`fixedUpdate` 限制 tag 区间（复用 [NpcFrameComponent.fixedUpdate](file:///e:/se/BlindDuel/scripts/Components/NpcFrameComponent.js#L98-L123) 逻辑）
- 2.2 `blocker` 字段 + `getBlockerAabb()`（§2.4）
- 2.3 `depthMask` 字段 + stencil plane 挂 root + attach（§2.5）
- 2.4 `stateMap`/`initialClip` 字段存储 + dispose 扩展（销毁 stencil plane + entity 级 texture）
- 2.5 多文件模式回归（rabble_flee 仍正常）

**Phase 3 — Scene 驱动**（3.1-3.3 stateMap 链路与 3.4-3.6 walkArea 链路可并行）
- 3.1 `_applyStateMap(entity)` 方法（§3.1）
- 3.2 `_spawnEntity` 末尾调 `_applyStateMap`
- 3.3 `_onWorldStateChange` 扩展：stateMap 重评估循环
- 3.4 `_applyWalkArea()` 方法（§3.2）
- 3.5 `Scene.init` walkArea 创建走 `_applyWalkArea` + 兼容旧 `walkArea` 单对象
- 3.6 `_onWorldStateChange` 扩展：walkarea 重评估

**Phase 4 — ExploreMode 收集**（依赖 2.2）
- 4.1 [_buildIndices](file:///e:/se/BlindDuel/scripts/Systems/Modes/ExploreMode.js#L421-L449) 收 `kind==="prop"` 且有 `getBlockerAabb`

**Phase 5 — CharacterFactory 透传**（依赖 Phase 2 全部，**最后改工厂**降低回归面）
- 5.1 `createPropEntity` 识别 entityDef 字段并透传：`blocker`/`depthMask`/`stateMap`/`initialClip`/`spriteSheetUrl`/`atlasData`/`clips`（区分单 tag 模式 vs 多文件模式）

**Phase 6 — SceneDef + NpcDefs 配置**（依赖 Phase 5 + §10.0 决策）
- 6.1 AssetManifest 注册 `altar` atlas + `altar_gem` item
- 6.2 prologue.json 加 altar entity（blocker + depthMask + stateMap + 单文件 tag clips：`init`/`completed`）
- 6.3 prologue.json 替换 dagger → altar_gem 拾取物
- 6.4 prologue.json 加 `walkAreas`（保留 `walkArea` 单对象作 fallback 兼容验证）
- 6.5 NpcDefs Charlotte priority 90：item id 改 altar_gem + `giveItem` 决策落地

**Phase 7 — 端到端验证**
- 7.1 主流程：拾取 altar_gem → 走近 Charlotte → 触发 action → scenario=105 → 祭坛切 `completed` + walkArea 扩展右侧 + hero 可继续向右
- 7.2 状态解耦验证：dev 直接改 scenario=105（未拾取）→ 祭坛仍切 clip（stateMap 与拾取链路独立）

### 10.3 commit 粒度约定

- 每个 Phase 内标"可并行"的步骤可同 commit；跨 Phase 必须按序
- 每个 commit 对应一个可观察现象（不按文件粒度切）：
  - "PropEntity 支持单文件多 tag"
  - "PropEntity blocker 上路"
  - "stateMap 首次 spawn 评估"
  - "walkArea 动态扩展"
- 5.1（createPropEntity 改造）放最后做：工厂一旦改错会同时影响 prop_faller + altar，等 PropEntity 各能力都单测过再动工厂风险最低